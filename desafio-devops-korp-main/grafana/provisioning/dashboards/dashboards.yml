apiVersion: 1

providers:
  - name: "Projeto Korp"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    options:
      # Diretório onde os arquivos .json de dashboard são montados no container.
      path: /var/lib/grafana/dashboards
