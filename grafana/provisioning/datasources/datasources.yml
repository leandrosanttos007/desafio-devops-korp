apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    # Nome do serviço Prometheus na rede Docker + porta padrão.
    url: http://prometheus:9090
    isDefault: true
    editable: true
